<td>{{article.description}}</td>
<td>{{article.notes}}</td>
<td>{{article.createdAt}}</td>
{{!-- Here we use the  `Ember.Select` view to render an HTML select element and
      bind it to the value of a given property. The `content` value are the 
      available options, the `selection` is what attribute to bind the result
      to. This list is found in `controllers/articles/index.js` (p. 70) --}}
<td>{{view "select" content=articleStates selection=article.state}}</td>
<td>
  {{!-- The `isSaving` and `isDirty` properties are baked into Ember. Others
        include:  isDeleted, isEmpty, isError, isLoaded, isLoading, isNew, 
                  isReloading, and isValid --}}
  {{#if article.isSaving}}
    <p>Saving...</p>
  {{else}}
    {{#if article.isDirty}}
      <button {{action "saveArticle" article}}>Save</button>
    {{/if}}
  {{/if}}
</td>
